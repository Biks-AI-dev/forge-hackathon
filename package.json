{
  "name": "notula",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "AI meeting scribe: live transcription -> structured notes -> PRD -> working UI prototype (Kimi on Nosana + Daytona sandboxes)",
  "engines": { "node": ">=20" },
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.21.0"
  }
}
