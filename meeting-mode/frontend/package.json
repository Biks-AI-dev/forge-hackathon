{
  "name": "meeting-mode-frontend",
  "version": "1.0.0",
  "description": "Biks Forge Meeting Mode — browser recording UI (PRD §2 item 1)",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^7.0.2",
    "vite": "^8.1.5"
  }
}
